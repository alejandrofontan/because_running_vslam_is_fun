Draft the next post in the "Because running VSLAM is fun" LinkedIn series.

1. Run `ls posts/` to find all existing post files and determine the next episode number (e.g. if the last is `1_3`, the next is `1_4`).
2. Read the two most recent posts to internalize the format, tone, and structure.
3. Ask the user for the details needed to fill in the new post:
   - Dataset or location used
   - VSLAM systems run (e.g. AllFeature-VSLAM, DROID-SLAM, MASt3R-SLAM)
   - Feature types coupled (e.g. ORB, ALIKED, SuperPoint, SIFT)
   - The single reproducibility command
   - Video link (if available)
   - Acknowledgements / people to thank
   - Any other notable context or story for the post body
4. Draft the post following the exact conventions:
   - Title line: `Because running VSLAM is fun (1.X) 📷🧭📍`
   - Body: dataset/location story, systems used, features coupled, reproducibility command block
   - Acknowledgements section
   - `Previous Post:` link line (use the previous post's LinkedIn URL if known, otherwise leave a placeholder)
   - `Next Post:` placeholder
   - Hashtag block consistent with the series
5. Write the draft to `posts/because_running_VSLAM_is_fun_1_X` (with the correct episode number).
6. Show the user the draft and ask for feedback before finalising.

$ARGUMENTS
